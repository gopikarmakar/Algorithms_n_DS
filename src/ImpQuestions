1) Implement a queue with two stacks so that each queue operations takes a constant amortized number of stack operations.

2) Create a data structure that efficiently supports the stack operations (push and pop) and also a return-the-maximum operation. Assume the elements are reals numbers so that you can compare them.

3) Explain why Java prohibits generic array creation ?

4) Given an array, build a BST. My chosen language is C ++. Q2. Given 10 GB data and 2 GB ram, how to sort?

5) We are calling a web service method that will sometimes fail. The problem is to change the client so that if there are 5 failures in less than 10 seconds, the client should stop attempting to make the call to the web method and just return a failure right away.

6) Design a wishlist system with existing API.

7) Serialization/Deserialization on tree

8) Look up in nokia phone  